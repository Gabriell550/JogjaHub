// Wrapper peta dipakai di customer/catalog & customer/vendor-detail untuk nampilin pinpoint vendor.
import React from 'react';
type Props = { latitude: number; longitude: number };
export function MapPreview({ latitude, longitude }: Props) {
  // TODO: render react-native-maps MapView dengan marker di latitude/longitude
  return null;
}
